# 永續力

12 單元的雙語永續職涯實作課，公開路徑為 `/course/sustainability/`。

```powershell
npm ci
npm run lint
npm test
```

本課採用與站內其他課程一致的 Next.js App Router 與靜態匯出架構。進度、練習與字級偏好儲存在瀏覽器 `localStorage`；討論區需要 `.env.local` 中的四個公開 giscus 變數。
